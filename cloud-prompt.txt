Call the RemoteTrigger tool with action "list" (paginate if has_more is true). Output ONLY a JSON array, one object per routine, with exactly these keys: id, name, enabled (boolean), cron_expression, model (derived_state.model), next_run_at, last_fired_at, last_run_status (last_run.status or null), last_run_finished_at (last_run.finished_at or null), connectors (array of mcp_connections[].name), prompt_head (first 240 characters of derived_state.prompt). No prose, no markdown, no code fence — the raw JSON array only.
